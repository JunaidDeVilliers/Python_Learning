name = input("Enter your full name: ")

result = name.isdigit() # will return true of false based on wether the entry is all digits or not
result2 = name.isalpha() # will return true of false based on wether the entry is all alphabetic characters or not

print(result)
print(result2)